# AI Provider - Claude via Vertex AI (Google Cloud)
# Required for AI query features

# Vertex AI Configuration
GOOGLE_APPLICATION_CREDENTIALS=/path/to/gcloud-credentials.json
VERTEX_PROJECT_ID=your-gcp-project-id
VERTEX_REGION=us-central1
CLAUDE_CODE_USE_VERTEX=1

# Dgraph Configuration
DGRAPH_URL=http://localhost:8080

# Better Auth
BETTER_AUTH_SECRET=change_this_in_production

# Database (optional, defaults to ./server/data.db)
# DATABASE_URL=./server/data.db

# Authentication Configuration
# AUTH_PASSWORD_ENABLED=true  # Set to "false" to disable password auth (OAuth only)
# AUTH_ALLOWED_EMAIL_DOMAINS=redhat.com,ibm.com  # Comma-separated list of allowed email domains (leave empty to allow all)

# Admin User Management
# ADMIN_EMAILS=admin@example.com,another.admin@example.com  # Comma-separated list of admin email addresses

# (Optional) Configure GitHub OAuth
# GITHUB_CLIENT_ID=your_client_id
# GITHUB_CLIENT_SECRET=your_client_secret
